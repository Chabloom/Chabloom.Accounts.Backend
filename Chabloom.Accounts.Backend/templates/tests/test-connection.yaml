apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Chabloom.Accounts.Backend.fullname" . }}-test-connection"
  labels:
    {{- include "Chabloom.Accounts.Backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Chabloom.Accounts.Backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
